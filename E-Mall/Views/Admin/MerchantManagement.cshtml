@model IEnumerable<E_Mall.Models.Merchant>

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "إدارة التجار";
}

<link rel="stylesheet" href="~/css/MerchantManagement.css" />

<div class="delivery-driver-management-container">
    <h1 class="text-center mb-4">@ViewData["Title"]</h1>

    <div class="d-flex justify-content-between align-items-center mb-3">
        <a asp-action="CreateM" class="btn btn-primary">
            <i class="fas fa-plus"></i> إضافة تاجر جديد
        </a>
    </div>
    <div class="delivery-driver-management-container">
        <form asp-action="MerchantManagement" method="get" class="form-inline">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="ابحث عن..." name="SearchString" value="@ViewData["CurrentFilter"]" />
                <div class="input-group-append">
                    <button class="btn btn-outline-secondary" type="submit" style="padding-bottom:10px;">
                        <i class="fas fa-search"></i> بحث
                    </button>
                    <a asp-action="MerchantManagement" class="btn btn-outline-secondary">
                        <i class="fas fa-sync-alt"></i>
                    </a>
                </div>
            </div>
        </form>
    </div>

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-6 col-lg-4">
                <div class="driver-card">
                    <div class="driver-info">
                        <h2 class="driver-name">@Html.DisplayFor(modelItem => item.MerchantName)</h2>
                        <p class="driver-details">
                            <i class="fas fa-envelope"></i> @Html.DisplayFor(modelItem => item.MerchantEmail)<br />
                            <i class="fas fa-phone"></i> @Html.DisplayFor(modelItem => item.MerchantPhone)<br />
                            <i class="fas fa-check-circle"></i> @(item.IsActive ? "نشط" : "غير نشط")
                        </p>
                    </div>
                    <div class="actions">
                        <a asp-action="EditM" asp-route-id="@item.Id">تعديل</a>
                        <a asp-action="DeleteM" asp-route-id="@item.Id">حذف</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>